<h1>Jobs</h1>

<table>
  <thead>
    <tr>
      <th>Job</th>
      <th>Description</th>
      <th>Complete</th>
      <th>Who</th>
    </tr>
  </thead>

  <tbody id="tbody">
    <% @jobs.each do |job| %>
      <tr>
        <td><%= link_to job.title, job_path(job) %></td> <td><%= job.description %></td>
        <td>
          <% if job.completed == false%>
            <p>On the List</p>
          <% elsif job.pending == true%>
            <p>Working Process</p>
          <% else %>
            <p>Done</p>
          <% end %>
        </td>
        <% if job.worker_id == nil %>
          <td id="normal"><p>Available <%= link_to "Claim", job_path(job), :method => "patch", remote: true %></p></td>
        <% else %>
        <td><%= Worker.find(job.worker_id).email %></td>
        <% end %>
        <td id="replace"></td>
      </tr>
    <% end %>
  </tbody>
</table>
